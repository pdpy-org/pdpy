/* 
 * This file is part of the 'pdpy' Pure Data / Python help files
 *
*/

(# Loading Abstractions

  # fill a table
  \my_Tab = 0.2 0.1 0.4 0.6 0.13 0.2 0.7 0.9 0.2 0.5

  # the .size() method gets the table size
  \my_Tab.size 

  # import the count.pdpy abstraction
  .count // see count.pdpy. The input is the counter's modulo

  # the .get() method gets values starting the given index (defualt is 1)
  \my_Tab.get > print
)